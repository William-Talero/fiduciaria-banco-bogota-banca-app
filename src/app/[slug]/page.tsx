"use client";
import React from "react";

export default function Restaurant({ params }: { params: { slug: string } }) {
  return (
    <main className="w-full flex min-h-svh flex-col items-center relative"></main>
  );
}
